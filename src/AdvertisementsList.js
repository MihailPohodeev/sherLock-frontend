import React, { useState, useEffect, useRef } from 'react';
import { pageState } from './pageState';
import config from './config';

function AdvertisementsList({ togglePage }) {
    return (
        <div>

        </div>
    );
}

export default AdvertisementsList;